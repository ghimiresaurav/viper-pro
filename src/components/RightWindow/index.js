import { Box } from "@mui/material";

const RightWindow = () => {
  return (
    <Box
      sx={{
        width: "80%",
        height: "100%",
        // backgroundColor: "green",
      }}
    >
      right
    </Box>
  );
};

export default RightWindow;
